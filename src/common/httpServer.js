/**
 * 
*/
// 数据请求地址 通过全局变量
import axios from 'axios';

export default class HttpServer {
  /**
   * 获取公共的post请求
   * @param {method} 请求方式
   * @param {url} 请求地址
   * @param {data} 请求数据
   * @param {loading} 是否执行加载图标
   * @returns {Promise} 异步对象
  */
 /**
  * 获取公共post请求
  * @param params
  * @return {Promise<any>}
 */
}